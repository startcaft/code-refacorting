***最好不要在另一个对象的属性基础上运用switch语句***
1，在Rental类中的getCharge()方法中的switch语句中，
		最好不要在另一个对象的属性基础上运用switch语句，
		如果不得不使用，也应该在对象自己的数据上使用，而不是在别人的数据上使用。
		
		【这也就意味着Rental类中的getCharge()应该归置到Movie类中】
		
		归置完getCharge()之后，以相同的手法处理积分计算。
		这样我们就把根据应影片类型而变化的所有东西，都放到了影片类型所属的类中了。

