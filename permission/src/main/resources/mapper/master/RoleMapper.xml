<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.basic.core.dao.master.RoleDao">

    <resultMap id="BaseResultMap" type="com.basic.core.entity.Role">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="description" jdbcType="VARCHAR" property="description"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="seq" jdbcType="INTEGER" property="seq"/>
        <result column="app_id" property="appId" jdbcType="BIGINT"/>
    </resultMap>

    <sql id="Base_Column_List">
      id, description, name, seq, app_id
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_role
        where id = #{id,jdbcType=BIGINT}
    </select>

    <select id="selectListDynamic" parameterType="map" resultMap="BaseResultMap">
        select 
        <include refid="Base_Column_List"/>
        from sys_role
        <where>
            <if test="roleName != null and roleName != ''">
                <bind name="_roleName" value="'%'+roleName+'%'"/>
                and name like #{_roleName}
            </if>
        </where>
    </select>

    <insert id="insert" parameterType="com.basic.core.entity.Role">
        insert into sys_role (id, description, name,
        seq)
        values (#{id,jdbcType=BIGINT}, #{description,jdbcType=VARCHAR}, #{name,jdbcType=VARCHAR},
        #{seq,jdbcType=INTEGER})
    </insert>

    <update id="updateByPrimaryKey" parameterType="com.basic.core.entity.Role">
        update sys_role
        set description = #{description,jdbcType=VARCHAR},
        name = #{name,jdbcType=VARCHAR},
        seq = #{seq,jdbcType=INTEGER}
        where id = #{id,jdbcType=BIGINT}
    </update>


    <!--中间表的操作-->
    <delete id="deleteRoleRes" parameterType="java.lang.Long" >
        delete from
        sys_role_resource
        where
        role_id = #{id,jdbcType=BIGINT}
    </delete>

    <insert id="insertRoleRes">
        insert into sys_role_resource (role_id,resource_id)
        values (#{roleId,jdbcType=BIGINT},#{resId,jdbcType=BIGINT})
    </insert>

</mapper>